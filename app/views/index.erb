
    <div class="container">
      <div class="row">
        <% @tasks.each do |task| %>
        <div class="col-md-4">
          <h3><%= task.id %>: <a href="/tasks/<%= task.id %>"><%= task.title %></a></h3>
        </div>
        <% end %>
      </div>
    </div>
